import ts = require("typescript/lib/tsserverlibrary");
/**
 * Represents the plugin-module.
 */
export declare class PluginModule implements ts.server.PluginModule {
    /**
     * The typescript-server.
     */
    private typescript;
    /**
     * The projects and their plugin.
     */
    private plugins;
    /**
     * The configuration of the plugins.
     */
    private config;
    /**
     * Initializes a new instance of the `PluginModule` class.
     *
     * @param typescript
     * The typescript-server.
     */
    constructor(typescript: typeof ts);
    /**
     * Gets the typescript-server.
     */
    get TypeScript(): typeof ts;
    /**
     * Creates a decorated language-service.
     *
     * @param createInfo
     * Information for the plugin.
     *
     * @returns
     * The newly created language-service.
     */
    create(createInfo: ts.server.PluginCreateInfo): ts.LanguageService;
    /**
     * Occurrs when the configuration is about to change.
     *
     * @param config
     * The configuration to apply.
     */
    onConfigurationChanged?(config: any): void;
}
