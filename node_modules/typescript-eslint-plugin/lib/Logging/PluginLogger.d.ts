import { Plugin } from "../Plugin";
import { LogLevel } from "./LogLevel";
import { LoggerBase } from "./LoggerBase";
/**
 * Provides the functionality to print log messages according to the configuration of the plugin.
 */
export declare class PluginLogger extends LoggerBase {
    /**
     * The plugin.
     */
    private plugin;
    /**
     * Initializes a new instance of the `Logger` class.
     *
     * @param plugin
     * The plugin.
     *
     * @param category
     * The category of the logger.
     */
    constructor(plugin: Plugin, category?: string);
    /**
     * Gets the plugin.
     */
    protected get Plugin(): Plugin;
    /**
     * Gets the configuration of the plugin.
     */
    get LogLevel(): LogLevel;
    /**
     * Writes a message to the log.
     *
     * @param message
     * The message to write.
     */
    protected Write(message: string): void;
}
