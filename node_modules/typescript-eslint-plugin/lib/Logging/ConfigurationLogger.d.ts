import { LogLevel } from "./LogLevel";
import { LoggerBase, SubLogger } from "./LoggerBase";
/**
 * Represents a logger for logging information about the configuration class.
 */
export declare class ConfigurationLogger extends SubLogger {
    /**
     * The log-level of the logger.
     */
    private logLevel;
    /**
     * Initializes a new instance of the `ConfigurationLogger` class.
     *
     * @param logLevel
     * The log-level of the logger.
     *
     * @param logger
     * The logger to use as base.
     *
     * @param category
     * The category of the logger.
     */
    constructor(logLevel: LogLevel, logger: LoggerBase, category?: string);
    /**
     * @inheritdoc
     */
    get LogLevel(): LogLevel;
    /**
     * Creates a parent logger for the `ConfigurationLogger`.
     *
     * @param logger
     * The logger to use as base.
     *
     * @returns
     * The parent loger for the `ConfigurationLogger`.
     */
    private static GetParentLogger;
}
