import { Plugin } from "../Plugin";
import { LogLevel } from "./LogLevel";
import { PluginLogger } from "./PluginLogger";
/**
 * Provides the functionality to print log messages according to the specified log-level.
 */
export declare class Logger extends PluginLogger {
    /**
     * The log-level to print.
     */
    private logLevel;
    /**
     * Initializes a new instance of the `Logger` class.
     *
     * @param plugin
     * The plugin.
     *
     * @param logLevel
     * The log-level to print.
     *
     * @param category
     * The category of the logger.
     */
    constructor(plugin: Plugin, logLevel?: LogLevel, category?: string);
    /**
     * @inheritdoc
     */
    get LogLevel(): LogLevel;
}
