"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ConfigurationLogger = void 0;
const LoggerBase_1 = require("./LoggerBase");
/**
 * Represents a logger for logging information about the configuration class.
 */
class ConfigurationLogger extends LoggerBase_1.SubLogger {
    /**
     * Initializes a new instance of the `ConfigurationLogger` class.
     *
     * @param logLevel
     * The log-level of the logger.
     *
     * @param logger
     * The logger to use as base.
     *
     * @param category
     * The category of the logger.
     */
    constructor(logLevel, logger, category) {
        super(ConfigurationLogger.GetParentLogger(logger), category);
        this.logLevel = logLevel;
    }
    /**
     * @inheritdoc
     */
    get LogLevel() {
        return this.logLevel;
    }
    /**
     * Creates a parent logger for the `ConfigurationLogger`.
     *
     * @param logger
     * The logger to use as base.
     *
     * @returns
     * The parent loger for the `ConfigurationLogger`.
     */
    static GetParentLogger(logger) {
        let categoryPath = [];
        while (logger instanceof LoggerBase_1.SubLogger) {
            categoryPath.push(logger.Category);
            logger = logger.Parent;
        }
        for (let i = categoryPath.length - 1; i >= 0; i--) {
            logger = new LoggerBase_1.SubLogger(logger, categoryPath[i]);
        }
        return logger;
    }
}
exports.ConfigurationLogger = ConfigurationLogger;
//# sourceMappingURL=ConfigurationLogger.js.map